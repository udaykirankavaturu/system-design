# system-design

common techniques and algorithms

# to run structurizer diagrams

docker run -it --rm -p 8080:8080 -v <PATH TO FOLDER where workspace files are present>:/usr/local/structurizr structurizr/lite
